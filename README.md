# Currency Converter App

Run locally:

```powershell
cd c:\Users\USER\.vscode\wdd330-finalproject
npm install
npm run start
```

This app uses https://exchangerate.host/ (no API key required) for demo purposes.
